// Este componente funcional exibe uma mensagem simples de "Carregando...".
// Pode ser usado para indicar um estado de carregamento na interface do usuário.
// vai servir para todas as partes da aplicacao
export default function loading() {
  return <p>Carregando...</p>
}
